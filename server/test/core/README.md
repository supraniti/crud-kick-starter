# Server Core Lane Scaffold

Core lane scaffold directory.

- lane: `core-fast`
- scope: engine/runtime contracts that are module-agnostic
- note: current executable tests remain in existing `server/test/*.test.js` and `server/test/core.contract/*` until lane consolidation is complete.
